// WRITE UNIT TESTS FOR FOOTER CONTROLLER HERE
import FooterController from './footer.controller';

describe('All of Footer controller /', () => {
    let $ctrl;

    beforeEach(() => {
        $ctrl = new FooterController();
    });

    it('should have a defined controller ...', () => {
        expect(FooterController).toBeDefined();
    });

});
