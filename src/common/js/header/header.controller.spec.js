// WRITE UNIT TESTS FOR HEADER CONTROLLER HERE
import HeaderController from './header.controller';

describe('All of Header controller /', () => {
    let $ctrl;

    beforeEach(() => {
        $ctrl = new HeaderController();
    });

    it('should have a defined controller ...', () => {
        expect(HeaderController).toBeDefined();
    });

});
