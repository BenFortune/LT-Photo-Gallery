import PhotoGalleryController from './photo-gallery.controller.js';

const component = {
    controller: PhotoGalleryController,
    template: require('./photo-gallery.template.html')
};

export default component;
